import { READ_ARTICLE_DETAIL, READ_ARTICLE_LIST, READ_COMMENT_BY_ARTICLE_ID, ADD_COMMENT } from './Actions'

export function rootReducer(intialState = [], action){
    if(action.type === READ_ARTICLE_DETAIL){
        //intialState = 
    }
}

